language: none
services:
 - elasticsearch
build:
 pre_ci_boot:
  image_name: vishnurao12/latest16
  image_tag: latest 
  pull: false
 ci:
  - curl localhost:9200
- shippable_service elasticsearch stop
