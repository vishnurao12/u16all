language: none
services:
 - redis
build:
 pre_ci_boot:
  image_name: vishnurao12/latest16
  image_tag: latest
  pull: false
 ci:
  - (echo "INFO"; echo -e "\nQUIT") | nc 127.0.0.1 6379
  - shippable_service redis stop
